# blog-cms

## Environment variables

Create a `.env.local` file in the project root and set:

- `NEXT_PUBLIC_AUTH_BASE_URL=https://services.auth.pattanaikdatapoint.com`
- `NEXT_PUBLIC_API_BASE_URL=` (set this to your non-auth API host if different)